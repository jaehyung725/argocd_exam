apiVersion: apps/v1
kind: Deployment
metadata:
  name: hostname-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webserver
  template:
    metadata:
      name: my-webserver
      labels:
        app: webserver
    spec:
      imagePullSecrets:          #docker hub 관련 정보 추가
      - name: dockerhub-secret #docker hub 관련 정보 추가
      containers:
      - name: my-webserver
      # 이미지는 본인 이미지로 변경 필요
        image: jaehyung725/echo-hostname:505ca50045c3a13c2ac141aef13899bdfd2426fb
        imagePullPolicy: Always
        ports:
        - containerPort: 8000